import { Box, Typography } from '@mui/material';

const Istatistikler = () => {
  return (
    <Box>
      <Typography variant="h4" sx={{ mb: 3 }}>İstatistikler</Typography>
      <Typography>İstatistik sayfası içeriği burada olacak</Typography>
    </Box>
  );
};

export default Istatistikler; 